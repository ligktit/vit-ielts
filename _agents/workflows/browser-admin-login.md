---
description: How to log in to the admin panel using the browser agent
---

# Browser Agent – Admin Login Instructions

Use the following steps when the browser agent needs to log in to the admin dashboard.

## Credentials

| Field    | Value                   |
| -------- | ----------------------- |
| Email    | techlead.holy@gmail.com |
| Password | IeltsTest@2026          |

## Steps

1. Navigate to the login page (e.g. `http://localhost:3000/login` or the deployed URL's `/login` route).
2. Enter the **email** `techlead.holy@gmail.com` into the email input field.
3. Enter the **password** `IeltsTest@2026` into the password input field.
4. Click the **Sign In** / **Login** button.
5. Wait for the page to redirect to the admin dashboard (e.g. `/admin` or `/admin/dashboard`).
6. Verify that the login was successful by checking for admin-specific UI elements (sidebar, admin menu, etc.).
